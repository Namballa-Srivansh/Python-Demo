import numpy as np

# array = np.zeros(10)
# array = np.zeros((2, 10))
# array = np.zeros((2, 3, 10))

# array = np.ones((2, 3, 10))
# array = np.full((2, 3, 10), 9) # fill the array with 9

# array = np.eye(3) # form identity matrices

# array = np.empty((2, 3))

# array = np.arange(0, 100, 0.1) # start, stop, step

array = np.linspace(0, 50, 6) # start, stop, num

print(array)