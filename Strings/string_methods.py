# name = input("Enter your Full Name : ")
# phone_num = input("Enter your Phone # : ")

# result = len(name) # Returns the length of string
# result = name.find("l") # Finds first occurence of the letter in the string
# result = name.rfind("a") # Finds the last occurence of the letter in the string
# name = name. capitalize() # Capitalize the first letter in the string
# name = name.upper() # convert the letters in the string into upper case letters
# name = name.lower() # convert the letters in the string into lower case letters
# result = name.isdigit() # return true if only digits are present in the string otherwise False
# result = name.isalpha() # returns true if only letters are present must not contain any space or digits
# result = phone_num.count("-") # count the occurence of given char
# phone_num = phone_num.replace("-", "") # Replaces the character with the other given character

# print(result)

# print(help(str)) 